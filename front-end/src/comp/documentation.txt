Website Url:
- https://big-pizza.onrender.com/

- localhost/api
    - /user_permissions/
        methods : GET
        description: 
             ALL Website Users (Admin - Subadmin - User/ Vistor)
        Response-Body:
                {
                    "id": NUMBER,
                    "password": STRING,
                    "last_login": DATE_TIME,
                    "is_superuser": BOOLEAN,
                    "email":STRING,
                    "is_staff": BOOLEAN,
                    "is_active": BOOLEAN,
                    "date_joined": DATE_TIME,
                    "groups": [],
                    "user_permissions": []
                }

- localhost/api/user/registration/
    methods : POST New User
    description:
    - ADD new User
    RequestBody:
            {

                "password": STRING, # REQUIERD
                "email":STRING, # REQUIERD
            }


- localhost/api/user/login/
    methods : POST 
    description:
        - LOGIN To The System 
    RequestBody:
            {
                "password": STRING, # REQUIERD 
                "email": STRING , # REQUIERD 
            }

- localhost/api/token/
    methods : POST 
    description:
        - Get User Token by sending his email and password in json requests
    RequestBody:
            {
                "password": STRING, # REQUIERD 
                "email": STRING , # REQUIERD 
            }

- localhost/api/token/refresh/
    methods : POST 
    description:
        - Get User access by sending refresh tocken in json requests
    RequestBody:
            {
                "refresh": STRING, # REQUIERD 
            }

- localhost/api/token/verify/
    methods : POST 
    description:
        - know if User access token is valid or not by sending 
            token in json requests if valid will return empty json {}
    RequestBody:
            {
                "token": STRING, # REQUIERD 
            }